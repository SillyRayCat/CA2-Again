<!-- Bao Rui-->
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Food Log</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container py-5">
      <h2 class="mb-4">Food Log</h2>
      <table class="table table-bordered">
        <thead>
          <tr>
            <th>Food Name</th>
            <th>Calories</th>
            <th>Carbs (g)</th>
            <th>Protein (g)</th>
            <th>Fats (g)</th>
            <th>Image</th>
          </tr>
        </thead>
        <tbody>
          <% for (var i = 0; i < foods.length; i++) { %>
            <tr>
              <td><%= foods[i].food_name %></td>
              <td><%= foods[i].calories %></td>
              <td><%= foods[i].carbs %></td>
              <td><%= foods[i].protein %></td>
              <td><%= foods[i].fats %></td>
              <td>
                <% if (foods[i].image) { %>
                  <img src="<%= foods[i].image %>" width="80" />
                <% } else { %>
                  No image
                <% } %>
              </td>
            </tr>
          <% } %>
        </tbody>
      </table>
      <a href="/dashboard" class="btn btn-secondary mb-3">← Back to Dashboard</a>
    </div>
  </body>
</html>
